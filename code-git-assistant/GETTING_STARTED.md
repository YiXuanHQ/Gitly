# 🚀 Git Assistant 开始使用指南

恭喜！您已成功搭建 Git Assistant 项目。本指南将帮助您快速上手。

## ✅ 当前状态

- ✅ 项目依赖已安装（293 packages）
- ✅ TypeScript 编译成功
- ✅ Webpack 打包成功
- ✅ 生成文件：
  - `dist/extension.js` (245 KB) - 扩展主体
  - `dist/webview/webview.js` (1.14 MB) - 可视化界面

## 📋 验证和调试步骤

### 第 1 步：启动开发监听（推荐）

在终端中运行：

```bash
cd E:\CodeGitAssistant
npm run watch
```

**预期输出：**
```
webpack 5.x watching the files…
webpack compiled successfully
```

💡 保持此终端运行，它会自动监听文件变化并重新编译。

---

### 第 2 步：启动调试

**方法 A：使用 F5 键（推荐）**

1. 在 VS Code 中打开 `E:\CodeGitAssistant` 文件夹
2. 按 `F5` 键
3. 或点击左侧 "运行和调试" 图标 → 点击绿色播放按钮

**方法 B：使用菜单**

- `运行` → `启动调试`
- 或 `Ctrl+F5`（不带断点）

**预期结果：**
- 会打开一个新的 VS Code 窗口
- 窗口标题显示 `[Extension Development Host]`
- 您的扩展在此窗口中已激活

---

### 第 3 步：验证扩展功能

#### 3.1 打开或初始化测试仓库

在 Extension Development Host 窗口中：

**路径 A：已有仓库**
```
文件 → 打开文件夹 → 选择包含 .git 的项目
```

**路径 B：使用 Git Assistant 初始化空文件夹**
1. 打开一个空目录（例如 `E:\TestGitRepo`）
2. 按 `Ctrl+Shift+P`，依次执行：
   - `Git Assistant: 初始化仓库`
   - `Git Assistant: 添加远程仓库`（可选）
   - `Git Assistant: 初始提交`
3. 完成后控制面板会显示远程、提交等信息

**路径 C：命令行手动创建（备用）**
```bash
mkdir E:\TestGitRepo
cd E:\TestGitRepo
git init
echo "# Test Repository" > README.md
git add .
git commit -m "Initial commit"
code .
```

#### 3.2 查看扩展是否激活

**方法 1：查看输出日志**
1. 按 `Ctrl+Shift+U` 打开输出面板
2. 从下拉菜单选择 "Git Assistant"
3. 应该看到：`Git Assistant 扩展已激活`

**方法 2：查看活动栏**
- 左侧活动栏应该有 Git Assistant 图标
- 点击图标查看侧边栏

#### 3.3 测试快捷键

| 快捷键 | 功能 | 预期结果 |
|--------|------|----------|
| `Ctrl+Shift+P` 输入 "Git Assistant" | 命令面板 | 显示所有 Git Assistant 命令 |
| `Ctrl+Alt+P` | 快速推送 | 弹出推送确认对话框 |
| `Ctrl+Alt+L` | 快速拉取 | 开始拉取操作（或提示设置远程仓库）|
| `Ctrl+Alt+B` | 切换分支 | 显示分支选择列表 |

#### 3.4 测试侧边栏视图

点击 Git Assistant 图标，查看：
- ✅ **分支管理** - 显示本地和远程分支
- ✅ **提交历史** - 显示最近的提交记录
- ✅ **冲突检测** - 显示冲突状态（无冲突时显示✅）

#### 3.5 测试控制面板

1. 按 `Ctrl+Shift+P`
2. 输入 `Git Assistant: 打开控制面板`
3. 回车

**预期结果：**
- 打开 Webview 面板
- 若当前文件夹未初始化，显示"初始化向导"并提供 `初始化仓库 / 添加远程 / 初始提交到本地仓库` 按钮
- 对已初始化仓库，显示 10 个标签页（按固定顺序，按钮宽度 120px）：
  1. 📋 快捷指令 - 命令历史与快速执行
  2. 📚 Git 指令集 - Git 命令速查
  3. 🌿 分支管理 - 分支树、创建/切换/合并
  4. ☁️ 远程仓库 - 添加/重命名/更新/删除远程
  5. 🏷️ 标签管理 - 创建带注释/轻量标签、推送/删除
  6. 🌳 分支视图 - 分支关系与合流路径
  7. ⚠️ 冲突解决 - 冲突列表、跳转、三栏对比
  8. 📊 提交图谱 - 高 DPI 2D 图形化提交历史
  9. 📅 时间线 - 日历热力图 + 柱状图
  10. 🔥 热力图 - 文件修改与贡献者活跃度分析

#### 3.6 测试一站式初始化流程

1. 在空文件夹打开控制面板 -> 点击 "🆕 初始化仓库"
2. 按提示添加远程地址（如 GitHub 仓库）
3. 点击 "📝 初始提交到本地仓库" 完成 `git add .`、`git commit`（本地提交，可选推送）
4. 刷新后应看到：
   - 远程仓库信息
   - 初始提交出现在提交历史中
   - 快捷操作按钮启用

---

## 🔍 调试技巧

### 使用断点调试

1. **设置断点**
   - 在源代码行号左侧点击设置红点
   - 例如在 `src/commands/git-operations.ts` 的第 20 行

2. **触发断点**
   - 在 Extension Development Host 中执行对应操作
   - 调试器会自动停在断点处

3. **查看变量**
   - 使用左侧调试面板查看变量值
   - 在调试控制台执行表达式

### 查看日志

**输出面板（推荐）：**
```
Ctrl+Shift+U → 选择 "Git Assistant"
```

**开发者工具：**
```
帮助 → 切换开发人员工具 → Console 标签
```

### Webview 调试

如果要调试 React 组件：

1. 打开控制面板
2. 在 Webview 内容区域右键
3. 选择 **"打开开发者工具"**
4. 使用 Chrome DevTools 调试

---

## 📝 功能验证清单

### 基础功能
- [ ] 扩展正常激活（查看输出日志）
- [ ] 侧边栏图标显示
- [ ] 命令面板中有所有命令
- [ ] 快捷键工作正常

### Git 操作
- [ ] 获取仓库状态
- [ ] 查看分支列表
- [ ] 创建新分支
- [ ] 切换分支
- [ ] 查看提交历史
- [ ] 通过 "初始化仓库 / 添加远程 / 初始提交到本地仓库" 完成首个 push

### UI 测试
- [ ] 分支树正常显示
- [ ] 提交历史列表显示
- [ ] 控制面板打开正常
- [ ] 样式正确（遵循 VS Code 主题）

---

## ❌ 常见问题

### 问题 1：扩展没有激活

**症状**：打开项目后看不到 Git Assistant

**原因**：当前文件夹不是 Git 仓库

**解决**：
- 按 `Ctrl+Shift+P` 执行 `Git Assistant: 初始化仓库`
- 或使用命令行 `git init`

### 问题 2：命令显示灰色不可用

**症状**：命令面板中的命令是灰色的

**原因**：不在 Git 仓库中或扩展未激活

**解决**：
1. 确保打开的是 Git 仓库
2. 重新加载窗口（`Ctrl+R`）

### 问题 3：快捷键不工作

**症状**：按 `Ctrl+Alt+P` 没有反应

**解决**：
1. 检查是否有键盘冲突（`文件 → 首选项 → 键盘快捷方式`）
2. 使用命令面板替代：`Ctrl+Shift+P` → 输入命令名

### 问题 4：控制面板空白

**症状**：打开控制面板后是空白页面

**解决**：
```bash
# 重新编译 webview
npm run compile

# 重新加载窗口
Ctrl+R (在 Extension Development Host 中)
```

### 问题 5：修改代码后没有生效

**解决**：
1. 确保 `npm run watch` 在运行
2. 在 Extension Development Host 中按 `Ctrl+R` 重新加载
3. 或重新按 `F5` 启动调试

---

## 🎯 下一步做什么

### 学习项目结构
阅读文档了解代码组织：
- [`PROJECT_OVERVIEW.md`](PROJECT_OVERVIEW.md) - 项目架构概览
- [`docs/DEVELOPMENT.md`](docs/DEVELOPMENT.md) - 开发指南
- [`docs/QUICKSTART.md`](docs/QUICKSTART.md) - 快速开始

### 开始开发

**添加新命令：**
1. 在 `src/commands/` 下添加逻辑
2. 在 `package.json` 的 `contributes.commands` 注册
3. 在 `src/commands/index.ts` 中绑定

**修改 UI：**
1. 编辑 `src/webview/components/` 下的 React 组件
2. 保存后自动重新编译（如果运行了 watch）
3. 刷新 Extension Development Host

**添加配置项：**
1. 在 `package.json` 的 `contributes.configuration` 添加
2. 在代码中使用：
```typescript
const config = vscode.workspace.getConfiguration('git-assistant');
const value = config.get('yourSetting');
```

### 测试真实场景

**场景 1：多分支开发**
```bash
git checkout -b feature/test-1
git checkout -b feature/test-2
# 使用 Git Assistant 切换分支
```

**场景 2：查看提交历史**
```bash
# 创建一些提交
echo "change 1" >> test.txt
git add . && git commit -m "Change 1"
echo "change 2" >> test.txt
git add . && git commit -m "Change 2"
# 使用 Git Assistant 查看历史
```

---

## 📦 打包扩展（可选）

如果想在其他 VS Code 中使用：

```bash
# 安装打包工具
npm install -g @vscode/vsce

# 打包扩展
vsce package

# 生成 git-assistant-1.0.1.vsix
```

安装：
1. 在 VS Code 中按 `Ctrl+Shift+P`
2. 输入 `Extensions: Install from VSIX...`
3. 选择生成的 `.vsix` 文件

---

## 🆘 需要帮助？

### 文档资源
- 📖 [VS Code Extension API](https://code.visualstudio.com/api)
- 📖 [TypeScript 手册](https://www.typescriptlang.org/docs/)
- 📖 [React 文档](https://react.dev/)

### 项目文档
- [`README_CN.md`](README_CN.md) - 详细功能说明
- [`CONTRIBUTING.md`](CONTRIBUTING.md) - 贡献指南

### 检查日志
```
Ctrl+Shift+U → Git Assistant → 查看详细日志
```

---

## 🎉 成功标志

如果您看到以下内容，说明一切正常：

✅ npm install 成功  
✅ npm run compile 成功  
✅ dist/extension.js 生成  
✅ dist/webview/webview.js 生成  
✅ F5 启动调试成功  
✅ Extension Development Host 窗口打开  
✅ 侧边栏显示 Git Assistant  
✅ 命令面板有 Git Assistant 命令  
✅ 控制面板可以打开  

**恭喜！您已经可以开始开发了！** 🎊

---

**祝您开发愉快！** 如有问题，请查看日志或参考文档。

